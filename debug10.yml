---
 - name: showing variables
   hosts: all
   vars:
     os: "linux"
     cloud: "aws"
     tools: "devops"
   tasks:
     - debug: msg="we will provide trainig {{os}}"
     - debug: msg="and lab experiance in {{cloud}}"
     - debug: msg="basic knowledge in {{tools}}"
